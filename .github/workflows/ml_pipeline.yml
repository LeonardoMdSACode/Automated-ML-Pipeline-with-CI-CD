name: ML Pipeline

on:
  push:
    branches: [ main ]
  pull_request:

jobs:
  ml-pipeline:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements-dev.txt
          pip install dvc

#      - name: Pull DVC data
#        run: dvc pull

      - name: Clean artifacts (models, reports, processed data)
        run: |
          rm -rf models
          rm -rf reports
          rm -rf data/processed

      - name: Run unit tests
        run: python -m pytest -m unit

      - name: Training
        run: python scripts/train.py

      - name: Evaluation
        run: python scripts/evaluate.py

      - name: Quality gate
        run: python scripts/compare.py

      - name: Package model
        run: python scripts/package_model.py

      - name: Run integration tests
        run: python -m pytest -m integration
